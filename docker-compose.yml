services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: intvyom-api
    ports:
      - "${PORT:-3000}:3000"
    env_file:
      - .env
    environment:
      PORT: ${PORT:-3000}
      MONGO_URI: ${MONGO_URI}
    restart: unless-stopped
